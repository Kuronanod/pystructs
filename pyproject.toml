[build-system]
requires = ["setuptools>=61.0" , "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "structcore"
version = "1.0.1"
description = "A comprehensive Python library for Data Structures* and *Algorithms."
readme = "README.md"
authors = [
    {name = "Thawankorn Suwannabon" , email = "thwalkrsuwrrnbl@gmail.com"}
]
license = {text = "MIT"}
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent"
]
requires-python = ">=3.8"

[project.urls]
Homepage = "https://github.com/Kuronanod/structcore"
Repository = "https://github.com/Kuronanod/structcore"

[tool.setuptools.packages.find]
where = ["."]
include = ["structcore*"]